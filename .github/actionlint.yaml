self-hosted-runner:
  labels:
    - neurodesk-syd-arcrunner-neurodesktop

config-variables: null

paths:
  .github/workflows/agentic-*.yml:
    ignore:
      # GitHub Actions supports queued concurrency; older actionlint does not.
      - 'unexpected key "queue" for "concurrency" section'
